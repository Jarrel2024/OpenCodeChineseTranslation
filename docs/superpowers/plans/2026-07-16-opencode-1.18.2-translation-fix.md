# OpenCodeChineseTranslation 适配 opencode 1.18.2 修复计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 OpenCodeChineseTranslation 项目的路径映射和字符串匹配，使其能正常汉化 opencode 1.18.2

**Architecture:** opencode 1.18.2 将 TUI 代码从 `packages/opencode/src/cli/cmd/tui/` 独立为 `packages/tui/src/`，同时部分组件被重命名或拆分为插件。修复涉及：修改 Go 语言汉化引擎的路径映射逻辑 + 更新 4 个翻译 JSON 的 file 路径 + 更新 2 条已变化的英文字符串 + 拆分侧边栏翻译配置。

**Tech Stack:** Go (CLI 工具), JSON (翻译配置), Bun (opencode 构建)

## Global Constraints

- 所有路径映射必须兼容旧版 opencode（保持向后兼容）
- 翻译 JSON 的 `file` 字段格式保持统一
- `config.json` 的 `upstream.version` 更新为 `1.18.2`
- 修复后 `verify` 命令通过率保持 100%
- 不删除或重命名现有翻译配置文件（只修改内容或新增）

---

### Task 1: 修复核心路径映射逻辑

**Files:**
- Modify: `cli-go/internal/core/i18n.go:199-211`

**Interfaces:**
- Consumes: `config.File` (源文件相对路径, 如 `src/cli/cmd/tui/component/dialog-model.tsx`)
- Produces: 正确的绝对路径指向 opencode 1.18.2 源码

- [ ] **Step 1: 修改 GetTargetFilePath 方法**

将原 `packages/opencode/` 固定前缀改为智能映射：对 `src/cli/cmd/tui/` 开头的路径，改为前缀 `packages/tui/src/`

```go
func (i *I18n) GetTargetFilePath(config TranslationConfig) string {
	relativePath := config.File
	if config.File == "" {
		return ""
	}
	if !strings.HasPrefix(relativePath, "packages/") {
		if strings.HasPrefix(relativePath, "src/cli/cmd/tui/") {
			rest := strings.TrimPrefix(relativePath, "src/cli/cmd/tui/")
			relativePath = filepath.Join("packages", "tui", "src", rest)
		} else {
			relativePath = filepath.Join("packages", "opencode", relativePath)
		}
	}
	return filepath.Join(i.opencodeDir, relativePath)
}
```

- [ ] **Step 2: 编译验证**

```powershell
cd cli-go
go build -o opencode-cli.exe .
```

预期：编译成功，无错误

- [ ] **Step 3: 运行 verify 命令测试**

```powershell
.\opencode-cli.exe --opencode-dir ..\opencode-1.18.2 verify
```

预期：大部分路径应该能找到对应文件，但部分文件重命名导致的失败（后续任务修复）

- [ ] **Step 4: 提交**

```bash
git add cli-go/internal/core/i18n.go
git commit -m "fix: update path mapping for opencode 1.18.2 TUI restructure"
```

---

### Task 2: 更新重命名的文件路径

**Files:**
- Modify: `cli-go/internal/core/assets/opencode-i18n/dialogs/dialog-command.json`
- Modify: `cli-go/internal/core/assets/opencode-i18n/routes/route-header.json`
- Modify: `cli-go/internal/core/assets/opencode-i18n/components/component-tips.json`

- [ ] **Step 1: 更新 dialog-command.json 的 file 路径**

原文 `src/cli/cmd/tui/component/dialog-command.tsx` → `command-palette.tsx`

```json
{
  "file": "src/cli/cmd/tui/component/command-palette.tsx",
  "description": "命令对话框",
  "replacements": {
    "category: \"Suggested\"": "category: \"建议\"",
    "title=\"Commands\"": "title=\"命令\""
  }
}
```

- [ ] **Step 2: 更新 route-header.json 的 file 路径**

原文 `src/cli/cmd/tui/routes/session/header.tsx` → `subagent-footer.tsx`

```json
{
  "file": "src/cli/cmd/tui/routes/session/subagent-footer.tsx",
  "description": "会话页头部/子智能体底部栏",
  "replacements": {
    "Subagent session": "子智能体会话",
    "Parent": "父会话",
    "Prev": "上一个",
    "Next": "下一个"
  }
}
```

- [ ] **Step 3: 更新 component-tips.json 的 file 路径**

原文 `src/cli/cmd/tui/component/tips.tsx` → `feature-plugins/home/tips-view.tsx`

保留所有 101 条替换规则不变，仅更新 `file` 字段。

- [ ] **Step 4: 运行 verify 验证**

```powershell
.\opencode-cli.exe --opencode-dir ..\opencode-1.18.2 verify
```

预期：上述 3 个文件路径匹配成功

- [ ] **Step 5: 提交**

```bash
git add cli-go/internal/core/assets/opencode-i18n/dialogs/dialog-command.json
git add cli-go/internal/core/assets/opencode-i18n/routes/route-header.json
git add cli-go/internal/core/assets/opencode-i18n/components/component-tips.json
git commit -m "fix: update file paths for renamed components in opencode 1.18.2"
```

---

### Task 3: 更新已变化的英文字符串

**Files:**
- Modify: `cli-go/internal/core/assets/opencode-i18n/dialogs/dialog-fork.json`
- Modify: `cli-go/internal/core/assets/opencode-i18n/routes/route-sidebar.json`

- [ ] **Step 1: 更新 dialog-fork.json 的字符串**

```json
{
  "file": "src/cli/cmd/tui/routes/session/dialog-fork-from-timeline.tsx",
  "description": "Fork对话框",
  "replacements": {
    "title=\"Fork session\"": "title=\"从消息分支\""
  }
}
```

- [ ] **Step 2: 更新 route-sidebar.json 的 LSP 字符串**

```json
{
  "file": "src/cli/cmd/tui/routes/session/sidebar.tsx",
  "description": "会话侧边栏",
  "replacements": {
    ">Connected</Match>": ">已连接</Match>",
    ">Disabled</Match>": ">已禁用</Match>",
    ">Needs auth</Match>": ">需要认证</Match>",
    "Needs client ID": "需要客户端 ID",
    "} active": "} 个活跃",
    "} error": "} 错误",
    "LSPs are disabled": "LSP 已禁用",
    "LSPs will activate as files are read": "LSP 将在读取文件时激活"
  }
}
```

- [ ] **Step 3: 提交**

```bash
git add cli-go/internal/core/assets/opencode-i18n/dialogs/dialog-fork.json
git add cli-go/internal/core/assets/opencode-i18n/routes/route-sidebar.json
git commit -m "fix: update changed English strings in opencode 1.18.2"
```

---

### Task 4: 拆分侧边栏翻译配置（适配插件化重构）

**Files:**
- Modify: `cli-go/internal/core/assets/opencode-i18n/routes/route-sidebar.json`
- Create: `cli-go/internal/core/assets/opencode-i18n/components/sidebar-lsp.json`
- Modify: `cli-go/internal/core/assets/opencode-i18n/config.json` (modules 列表)

- [ ] **Step 1: 更新 route-sidebar.json 指向 mcp.tsx**

```json
{
  "file": "src/cli/cmd/tui/feature-plugins/sidebar/mcp.tsx",
  "description": "MCP 侧边栏插件",
  "replacements": {
    ">Connected</Match>": ">已连接</Match>",
    ">Disabled</Match>": ">已禁用</Match>",
    ">Needs auth</Match>": ">需要认证</Match>",
    "Needs client ID": "需要客户端 ID",
    "} active": "} 个活跃",
    "} error": "} 错误"
  }
}
```

- [ ] **Step 2: 创建 sidebar-lsp.json**

```json
{
  "file": "src/cli/cmd/tui/feature-plugins/sidebar/lsp.tsx",
  "description": "LSP 侧边栏插件",
  "replacements": {
    "LSPs are disabled": "LSP 已禁用",
    "LSPs will activate as files are read": "LSP 将在读取文件时激活"
  }
}
```

- [ ] **Step 3: 更新 config.json 添加新的模块条目**

在 `modules.components` 数组末尾追加 `"components/sidebar-lsp.json"`

- [ ] **Step 4: 运行 verify 确认全部通过**

```powershell
.\opencode-cli.exe --opencode-dir ..\opencode-1.18.2 verify
```

预期：所有规则 100% 匹配

- [ ] **Step 5: 提交**

```bash
git add cli-go/internal/core/assets/opencode-i18n/routes/route-sidebar.json
git add cli-go/internal/core/assets/opencode-i18n/components/sidebar-lsp.json
git add cli-go/internal/core/assets/opencode-i18n/config.json
git commit -m "fix: split sidebar translations for plugin-based architecture in opencode 1.18.2"
```

---

### Task 5: 更新 config.json 元信息

**Files:**
- Modify: `cli-go/internal/core/assets/opencode-i18n/config.json`

- [ ] **Step 1: 更新版本和 commit 信息**

```json
{
    "upstream": {
        "version": "1.18.2",
        "supportedCommit": "HEAD"
    }
}
```

其他字段保持不变。

- [ ] **Step 2: 提交**

```bash
git add cli-go/internal/core/assets/opencode-i18n/config.json
git commit -m "chore: update config version to 1.18.2"
```

---

### Task 6: 最终验证

- [ ] **Step 1: 全量 verify 测试**

```bash
.\opencode-cli.exe --opencode-dir ..\opencode-1.18.2 verify
```

预期输出：
- 所有配置文件加载成功（41+ 个，含新增 sidebar-lsp.json）
- 所有替换规则匹配成功（~399 条）
- 无 warning 和 error

- [ ] **Step 2: 全量 apply 测试（dry-run）**

```bash
.\opencode-cli.exe --opencode-dir ..\opencode-1.18.2 apply --dry-run
```

预期：无错误提示，汉化规则报告正确
